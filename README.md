line-radio
==========

A minimal website for finding new music.

Setup
-----

```bash
gem install sass
npm install -g grunt-cli
npm install
bower install
```

then watch for changes with
```bash
grunt watch
```